	34. 객체지향 쿼리 언어1(기본 문법) - 소개
		- JPQL
			: 가장 단순한 조회 방법
				1. EntityManager.find()
				2. 객체 그래프 탐색 (a.getB().getC())
			: JPA를 사용하면 엔티티 객체를 중심으로 개발
			: 문제는 검색 쿼리
			: 검색을 할 때도 테이블이 아닌 엔티티 객체를 대상으로 검색
			: 모든 DB데이터를 객체로 변환해서 검색하는 것은 불가능
			: 애플리케이션이 필요한 데이터만 DB에서 불러 올려면 결국 검색 조건이 포함된 SQL이 필요
			
				- JPA는 SQL을 추상화한 JPQL이라는 객체 지향 쿼리 언어 제공
				- JPQL은 엔티티 객체를 대상으로, SQL은 테이블 대상으로
			
		- JPA Criteria
		- QueryDSL
		- 네이티브 SQL
		- JDBC API 직접 사용, MYBatis, SpringJdbcTemplate과 함께 사용
		
		
		