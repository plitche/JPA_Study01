	39. 객체지향 쿼리 언어1(기본 문법) - JPQL 함수
		- 기본 함수
		CONCAT
		SUBSTRING
		TRIM
		LOWER, UPPER
		LENGTH
		LOCATE
		ABS, SQRT, MOD
		SIZE, INDEX(JPA 용도)
		
		- 사용자 지정 함수
			: 사용하는 DB 방언을 상속받고 사용자 정의 함수를 등록해야 한다.
			